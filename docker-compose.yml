services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - shelflife-data:/app/data
    environment:
      - SHELFLIFE_DB_PATH=/app/data/shelflife.db
      - SHELFLIFE_LOCAL_USERNAME=local

  mcp:
    build:
      context: .
      args:
        MODE: mcp
    volumes:
      - shelflife-data:/app/data
    environment:
      - SHELFLIFE_DB_PATH=/app/data/shelflife.db
      - SHELFLIFE_LOCAL_USERNAME=local
    stdin_open: true

volumes:
  shelflife-data:
